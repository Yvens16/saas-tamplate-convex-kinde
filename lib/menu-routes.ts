import { routes } from "./routes"
import { GraduationCapIcon } from "lucide-react";

export const menuRoutes = [
  { icon: GraduationCapIcon, label: "example", href: routes.home, isProtected: false }
]